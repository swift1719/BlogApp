<%-include("partials/header")%>
<div class="ui main text container segment">
    <div class="ui huge header">Find blogs you are looking for...</div>
</div>

<%blogs.forEach((blog)=>{%>
    <div class="ui container segment">
        <h2><%=blog.title%></h2>
        <img class="ui image" src="<%=blog.image%>" alt="img">
        <span> <%= blog.created.toDateString()%></span>
        <p><%-blog.body.substring(0,100)%>  ...</p>
        <a href="/blogs/<%=blog._id%>" class="ui teal button">Read More</a>
    </div>
<%})%>
<%-include("partials/footer")%>